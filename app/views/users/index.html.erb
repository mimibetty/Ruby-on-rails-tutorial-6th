<% provide(:title, 'All users') %>

<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render partial: 'user', collection: @users, as: :user %>
</ul>

<%= will_paginate %>

<br>
<%= link_to 'New User', new_user_path %>